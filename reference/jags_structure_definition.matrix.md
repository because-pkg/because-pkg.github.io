# Method for JAGS Structure Definition (Matrix)

Method for JAGS Structure Definition (Matrix)

## Usage

``` r
# S3 method for class 'matrix'
jags_structure_definition(
  structure,
  variable_name = "err",
  optimize = TRUE,
  ...
)
```
