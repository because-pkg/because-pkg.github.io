# Custom NIMBLE Distributions for because

These functions provide custom distribution implementations for NIMBLE.

## Usage

``` r
dnb_because(...)

rnb_because(...)

dzip_because(...)

rzip_because(...)

dzinb_because(...)

rzinb_because(...)
```

## Arguments

- ...:

  Arguments passed to the distribution.
