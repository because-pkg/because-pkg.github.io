# Family Definition Generics and Default Methods

S3 generics and methods for distribution families in because. This
enables custom distributions to be added by defining S3 methods.

## Usage

``` r
.nimble_fn_cache
```

## Format

An object of class `environment` of length 0.
