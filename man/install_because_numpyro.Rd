% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_numpyro.R
\name{install_because_numpyro}
\alias{install_because_numpyro}
\title{Install NumPyro and Python dependencies for because}
\usage{
install_because_numpyro(
  envname = NULL,
  method = "auto",
  because_py_url = "https://github.com/because-pkg/because_py/archive/main.zip",
  ...
)
}
\arguments{
\item{envname}{The name, or full path, of the Python environment in which the Python packages
are to be installed. If \code{NULL} (the default), it automatically detects whether RStudio
or \code{reticulate} has forced a specific environment (e.g. via \code{VIRTUAL_ENV}) and
uses that. Otherwise, it defaults to \code{"r-reticulate"}.}

\item{method}{Installation method. By default, \code{"auto"} automatically finds a method that will work
in the local environment. Change the default to force a specific installation method
(e.g., \code{"virtualenv"}, \code{"conda"}, or \code{"pip"}).}

\item{because_py_url}{The GitHub repository URL for the \code{because_py} Python module.
Update this to the official repository link once hosted.}

\item{...}{Additional arguments passed to \code{reticulate::py_install()}.}
}
\description{
This helper function installs the necessary Python dependencies (\code{numpyro}, \code{jax})
and the companion Python package (\code{because_py}) required to run the \code{numpyro} engine in \code{because()}.
}
