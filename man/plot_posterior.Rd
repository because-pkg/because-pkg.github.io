% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_posterior.R
\name{plot_posterior}
\alias{plot_posterior}
\title{Plot Posterior Distributions for Model Comparison}
\usage{
plot_posterior(
  models,
  parameter,
  col = NULL,
  lwd = 2,
  legend_pos = "topleft",
  density_args = list(),
  ...
)
}
\arguments{
\item{models}{A \code{because} object or a named list of \code{because} objects.
If a list is provided, the names are used for the legend.
Example: \code{list("Default" = fit1, "Custom" = fit2)}.}

\item{parameter}{Character string. A regular expression or exact name of the parameter(s) to plot.
Example: \code{"beta"}, \code{"^alpha"}, \code{"beta_Y_X"}. Plots all matching parameters.}

\item{col}{Optional vector of colors. Defaults to a standard palette (black, blue, red, etc.).}

\item{lwd}{Line width (default = 2).}

\item{legend_pos}{Legend position (default = "topleft"). Set to \code{NULL} to suppress.}

\item{density_args}{List of additional arguments passed to \code{stats::density()}.
Useful for handling boundary corrections (e.g., \code{list(from=0)} for truncated priors).}

\item{...}{Additional arguments passed to \code{plot()}.}
}
\value{
Invisible NULL. Produces a plot.
}
\description{
Visualizes posterior density distributions for selected parameters, allowing easy comparison
between multiple models (e.g., Default vs. Custom priors) or inspection of a single model.
}
\examples{
\dontrun{
fit <- because(list(Y ~ X), data = my_data)
plot_posterior(fit, parameter = "beta_Y_X")
}

}
