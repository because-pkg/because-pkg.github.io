% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marginal_effects.R
\name{marginal_effects}
\alias{marginal_effects}
\title{Calculate Marginal Effects for Because Fit}
\usage{
marginal_effects(
  fit,
  at = NULL,
  prob = 0.95,
  samples = 100,
  multinomial_probabilities = FALSE
)
}
\arguments{
\item{fit}{A because fit object.}

\item{at}{Character or list. If NULL (default), calculates Average Marginal Effects (AME).
If "mean", calculates Marginal Effects at the Mean (MEM).}

\item{prob}{Numeric; probability mass for the credible interval (default 0.95).}

\item{samples}{Integer. Number of posterior samples to use (default 100 for speed).}

\item{multinomial_probabilities}{Logical. If TRUE, returns granular probability shifts for each category
of multinomial (unordered) responses instead of a single expected value shift. Default FALSE.}
}
\value{
A data frame with marginal effects per path.
}
\description{
Estimates the Average Marginal Effect (AME) or Marginal Effect at the Mean (MEM)
for all paths in a model. This is especially useful for comparing coefficients across
different families (e.g., Gaussian vs. Binomial) as it converts them to a common scale
(expected change in the response).
}
