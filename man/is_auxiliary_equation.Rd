% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/custom_extensions.R
\name{is_auxiliary_equation}
\alias{is_auxiliary_equation}
\title{Create a Custom Covariance Structure for use with because}
\usage{
is_auxiliary_equation(family, response, all_responses, ...)
}
\arguments{
\item{family}{A family object (S3 class \verb{because_family_*})}

\item{response}{Character; the response variable name}

\item{all_responses}{Character vector of all response variable names in the model}

\item{...}{Additional arguments}

\item{name}{Character string; the name of your structure (e.g., "spatial_knn").
This will be used to create the S3 class and methods.}

\item{precision_fn}{A function that takes your structure data and returns
a precision matrix (N x N). The first return value should be the precision matrix.
Additional list elements can be returned and will be passed to JAGS as data.}

\item{description}{Optional description of the structure for documentation.}
}
\value{
A constructor function that creates structure objects of your custom class.

A list: \code{list(skip_likelihood = FALSE, skip_variance = FALSE)}
}
\description{
This helper function allows users to easily create custom covariance structures
by providing just a function that computes the precision (or covariance) matrix.
The function handles all the S3 method registration automatically.
}
\details{
This function creates:
\itemize{
\item A constructor function (returned) for creating structure objects
\item S3 methods for \code{jags_structure_definition} (generates JAGS code)
\item S3 methods for \code{prepare_structure_data} (prepares data for JAGS)
}

The precision function should return either:
\itemize{
\item A single matrix (the precision matrix)
\item A list with \code{Prec} (the precision matrix) and optionally other data
}
}
