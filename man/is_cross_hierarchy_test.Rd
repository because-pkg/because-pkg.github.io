% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/because_dsep.R
\name{is_cross_hierarchy_test}
\alias{is_cross_hierarchy_test}
\title{Check if a d-sep test is a cross-hierarchy test (trivially satisfied)}
\usage{
is_cross_hierarchy_test(test_eq, hierarchical_info)
}
\arguments{
\item{test_eq}{A formula with attribute "test_var" naming the focal predictor.}

\item{hierarchical_info}{List with $levels (named list) and $hierarchy (string).}
}
\value{
TRUE if the test should be skipped; FALSE otherwise.
}
\description{
A test Response \emph{||} FocalPredictor | \code{\{...\}} is "cross-hierarchy" when the
response and the focal predictor live in orthogonal hierarchical branches
(e.g., one is species-level, the other is site-level) AND the conditioning
set does not contain a variable from a level that connects the two branches
(typically the observation level). Such tests are trivially independent by
design of the hierarchical data structure and cannot be run in JAGS because
no cross-level index exists between orthogonal branches.
}
\keyword{internal}
